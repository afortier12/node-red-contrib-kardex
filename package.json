{
  "name": "node-red-contrib-excel",
  "version": "1.0.0",
  "description": "import excel files and convert contents to json",
  "dependencies": {
    "exceljs": "^4.1.1",
    "fs": "0.0.1-security",
    "lodash": "^4.17.20",
    "node-red": "^1.1.3"
  },
  "node-red": {
    "nodes": {
      "import": "/pallets/excel-import/import.js"
    }
  },
  "nodemonConfig": {
    "ignore": [
      "*/build/"
    ]
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --watch pallets --watch src/client --exec 'npm run build-all'"
  },
  "author": "",
  "license": "ISC",
  "main": "index.js"
}
